<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MediaPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/iransans</item>
        <!--<item name="fontFamily">@font/iransans_regular</item>
        <item name="fontFamily">@font/iransans_buld</item>-->
    </style>

    <style name="Theme.MediaPlayer" parent="Base.Theme.MediaPlayer" />
</resources>